{
  "name": "@truebite/api",
  "version": "0.0.0",
  "private": true,
  "description": "TrueBite backend — /places/nearby orkestrasyon + cache katmanı.",
  "type": "module",
  "main": "./src/server.ts",
  "scripts": {
    "dev": "node --watch --env-file=.env src/server.ts",
    "start": "node --env-file=.env src/server.ts",
    "test": "node --test \"src/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.107.0",
    "@truebite/shared": "*",
    "@supabase/supabase-js": "^2.45.0",
    "fastify": "^5.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
