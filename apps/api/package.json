{
  "name": "@truebite/api",
  "version": "0.0.0",
  "private": true,
  "description": "Volicious backend — /places/nearby orkestrasyon + cache katmanı.",
  "type": "module",
  "main": "./src/server.ts",
  "scripts": {
    "dev": "node --watch --env-file=.env src/server.ts",
    "start": "node --env-file=.env src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@truebite/core": "*",
    "@truebite/shared": "*",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
